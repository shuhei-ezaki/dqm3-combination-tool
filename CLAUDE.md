# DQM3 ダークドレアム配合ルート図

`dqm3-darkdream-route.html` を編集・拡張するためのプロジェクトルール。

## 成果物

- `dqm3-darkdream-route.html` — 単一ファイル。Mermaid 10 を CDN から読み込み、タブ切り替えで各ルートを表示する。
- 依存なし。ブラウザで直接開ける。確認するときは `npx serve .` などで開いて目視する（Mermaid はターミナルではレンダリングできない）。

## 図の作成ルール

- 末端は必ず「通常配合（系統×系統＋ランク条件）」まで分解する。
- スカウト可能でも、通常配合ルートがあるなら通常配合を終端として記載する。スカウト場所は補足として併記。
- 通常配合ルートが存在しないモンスターは、スカウト可能ならスカウトを終端とし、不可なら特殊配合を辿り続ける。
- 通常配合で到達できないのはタマゴ限定モンスターのみ。その場合は赤で明示する。
- ランクは必ず括弧で明記する（例：`ブリザード(Dランク)`）。「?」や「ランク不明」は残さない。
- 矢印の向きは「素材 → 完成品」。

### 色

| 色 | 意味 | classDef |
|---|---|---|
| 金 | 最終目標 | `fill:#ffd700,stroke:#c9a000,color:#000` |
| 青 | 通常配合で作れる終端（通常配合が存在しない場合はスカウト終端） | `fill:#cfe8ff,stroke:#4a90d9,color:#000` |
| 赤 | タマゴ限定・配合で到達不可 | `fill:#ffd6d6,stroke:#d96a6a,color:#000` |
| 白 | 特殊配合（さらに辿る） | デフォルト |

### タブ構成

全体図 / 竜神王 / デスタムーア / ラーミア / 神さま / しんりゅう

1タブ内に `<h3>` + `.chart` を複数置いて枝ごとに分割してよい。1つの Mermaid 図が大きくなりすぎるとかえって読めない。

### 実装メモ

- タブは `data-target` と `.panel` の id を対応させ、初回表示時に `mermaid.run()` する遅延レンダリング。
- Mermaid のノードIDは1つの `flowchart` 内で一意であればよい（図をまたいで重複してよい）。
- ノード内改行は `<br/>`。括弧を含むラベルは必ずダブルクォートで囲む。

## DQM3 の配合仕様（重要）

- **通常配合はGランク〜Bランクのみ。** 各ランクに36通り（8系統から2つ選ぶ組み合わせ、同系統含む）が定義されている。
- 通常配合の結果は「親2体の系統ペア」で決まり、生まれるのは**どちらかの親の系統**のモンスター。ランクは親のランクで決まる。表記は `X系×Y系(Rランク以上)`。
- **Aランク以上は必ず特殊配合。** 通常配合では作れない。
- 4体配合は祖父母4体を2ペアに分けて両親を作る。両親の系統は問わない。
- 配合に使った素材モンスターは消費されて手元からいなくなる。同じ素材を2箇所で使う場合は2体作る必要がある。

## ランクの確定方法

図鑑No. は **系統 → ランク昇順 → 50音順** で並んでいる。ランクが既知のモンスターの図鑑No. で区切りを取れば、間のモンスターのランクが機械的に確定できる。

例（物質系）:

- Dランク帯 313〜324: キラーマシン(313) / サブナック(314) / しびれだんびら(316) / ストーンマン(317) / デビルスノー(318) …
- Cランク帯 325〜335: うごくせきぞう(325) / 王のはかもり(326) / … / ルバンカ(335)
- Bランク帯 336〜346: からくりだいみょう(336) / ギャスモン(337) / グレイトボンバー(338) / … / デュランダル(345) / ヘルシーサー(346)
- Aランク帯 347〜350: キラーマシン2(347) / Sキラーマシン(348) / てんのもんばん(349) / パンドラボックス(350)

系統ごとの図鑑No. 範囲（おおよそ）: スライム 1-63 / ドラゴン 64-134 / 自然 135-204 / 魔獣 205-277 / 物質 278-350 / 悪魔 351-425 / ゾンビ 426-496 / ???系 497-

新しいノードを追加するときも、まずこの方法でランクを確定してから図に入れること。

## 出典

- アルテマ 通常配合表（G〜Bランクの36通り一覧）: `https://altema.jp/dqm3/tujouhaigou`
- アルテマ 特殊配合一覧（図鑑No.付き）: `https://altema.jp/dqm3/tokusyuhaigou`
- アルテマ ランク別一覧: `https://altema.jp/dqm3/rank/3` （3=A、2=S、4=B …）
- GAME攻略BOX 個別ページ: `https://gamekoryakubox.com/dqm3/rank_g/g054/` のようにランク別ディレクトリ配下
- ゲームエイト / ゲームウィズ の個別モンスターページ

regamers.net と kyokugen.info は robots で自動取得不可。

## 確定済みの主要ルート

```
ダークドレアム(X) = 4体配合
  ├ 親A = デスタムーア(S) × ラーミア(X)
  └ 親B = 神さま(X) × 竜神王(X)   ← 竜神王は達成済み

竜神王(X)   = ひかりの番人(A) × 竜王(S)
デスタムーア(S) = 4体配合 [バトルレックス(B) × キラーマジンガ(S)] + [トロルボンバー(B) × ムドー(S)]
ラーミア(X) = 4体配合 [しんりゅう(X) × てんのもんばん(A)] + [ほうおう(A) × わたぼう(S)]
神さま(X)   = スライムエンペラー(A) × オルゴデミーラ(S)
しんりゅう(X) = ドラゴンアビス(S) × サラマンダー(A)
```

### タマゴ限定のボトルネック（2箇所のみ）

1. **スカイドラゴン(Fランク・白/銀タマゴ限定)** → スノードラゴン(D) → サラマンダー(A) → しんりゅう(X) → ラーミア(X)
2. **ピサロのてさき(銀タマゴ限定)** → なげきの亡霊(D) → ワイトキング(B) → ナイトキング(A) → オルゴデミーラ(S) → 神さま(X)

デスタムーア系・竜神王系にタマゴ限定は不要。

## 過去に間違えていた点（再発防止）

- ムドーの素材ホラービーストは **通常配合 悪魔系×魔獣系(B以上)** で作れる。ドルマージュ→マネマネ（タマゴ限定）の枝は不要。
- キラージャック / れんごく天馬 / アカイライ / バトルレックス / キングリザード なども通常配合の終端。Bランク以下は安易に「特殊配合」と決めつけない。
- ダンビラムーチョ = 魔獣系×悪魔系(C以上)。「悪魔系×物質系」ではない。
- サタンメイル = 物質系×悪魔系(B以上)。「悪魔系×スライム系」ではない。
- ナイトリッチ / トロルボンバー / サイレス は通常配合ではなく特殊配合。
- シャンタクの素材は キングリザード × ヘルコンドル。ヘルダイバーではない。
- シャンタクは Cランク（Dではない）。グレイトドラゴンは Bランク（Aではない）。
- ゴールデンゴーレム(No.339) はアルテマのランク別一覧でAと表示されているが、図鑑No. の並びからはBランク。一覧側の誤りと思われる。

## 未解決

- ユーザーから「モンスター名が実際と違うものがある」との指摘あり。具体的にどのノードかは未特定。図鑑No. との照合では不一致を見つけられていない。名前の疑いが出たら、同系統・同ランク帯の50音順リストから正しい候補を探すこと。
